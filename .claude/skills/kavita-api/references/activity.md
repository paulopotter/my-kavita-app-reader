# Kavita API — Activity

Schema types (`SomeDto`) referenced below are defined once in `schemas.md` — look them up there instead of expecting them inline here.

## GET /api/Activity/current
*Returns active reading sessions on the Server*

**Responses:**
**200** OK
```
[ReadingSessionDto]
```

**See schemas:** ReadingSessionDto

---
