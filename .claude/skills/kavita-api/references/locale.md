# Kavita API — Locale

Schema types (`SomeDto`) referenced below are defined once in `schemas.md` — look them up there instead of expecting them inline here.

## GET /api/Locale
*Returns all applicable locales on the server*

**Responses:**
**200** OK
```
[KavitaLocale]
```

**See schemas:** KavitaLocale

---
