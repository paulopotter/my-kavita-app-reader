---
task: 012 - Docs — commit with implementation documentation
plan: 002 - Splash Screen & Navigation
date: 2026-08-11
status: done
---

# 012 - Documentação do Plano 002

## O que foi entregue

Documentação completa do Plano 002 em três frentes: (1) três novos erros comuns adicionados em `.claude/docs/mistakes.md` — `ProcessLifecycleMarker` no `init {}`, `useState` vs `useRef` em closures de timer, e `Promise.all` para duração mínima paralela; (2) seção de funcionalidades disponíveis e tabela de políticas OTA adicionadas ao `README.md`; (3) seção `[Unreleased]` do `CHANGELOG.md` preenchida com adicionados/corrigidos separados por Backend e Frontend, em português, no padrão keep-a-changelog.

## Como foi testado

Revisão manual do conteúdo gerado. Os três mistakes documentam bugs reais que ocorreram e foram corrigidos durante o plano — verificados nos commits correspondentes (`StartupModule.kt`, `useSplash.ts`). O CHANGELOG foi conferido contra o padrão das entradas anteriores do arquivo.

## Aprovação

Usuário solicitou explicitamente: "Tudo, depois que terminar, marque a ultima task como done, e pode finalizar o plano" em 2026-08-11.

## Notas

- O CHANGELOG não inclui versão no `[Unreleased]` — a pipeline se encarrega de inserir a versão e data no momento do release.
- O `README.md` lista funcionalidades disponíveis como checklist marcada, separada das planejadas.
- A documentação de arquitetura (`architecture.md`) foi atualizada na mesma sessão com as seções `android/node_modules` (symlink) e assets gerados pelo Metro.
