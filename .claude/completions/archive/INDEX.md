# Completions Archive Index

| Plan | Description |
|------|-------------|
| [001 - Config Screen](001%20-%20Config%20Screen/README.md) | First vertical slice: Android scaffold + Kotlin tools (Room, bridge, network) + RN Config screen — verified running on physical device |
| [003 - Library Screen](003%20-%20Library%20Screen/README.md) | Library screen: BFF enrichment, i18n system, bottom navigation bar, splash sync, local reading-progress storage — all 14 tasks done |
| [002 - Splash Screen & Navigation](002%20-%20Splash%20Screen%20%26%20Navigation/README.md) | Animated RN splash overlay, react-navigation foundation, StartupModule, full route graph with placeholders — all 12 tasks done |
| [010 - OTA Infrastructure](010%20-%20OTA%20Infrastructure/README.md) | OTA bundle check + download, crash guard, rollback, policy matching, CI release pipeline — all 15 tasks done |
| [006 - Series Detail Screen](006%20-%20Series%20Detail%20Screen/README.md) | Series detail screen: chapter listing, multi-select, optimistic read/unread marking, 3-level chapter sort, SeriesModule bridge with reactive follow stream — all 11 tasks done |
| [007 - Reader Screen](007%20-%20Reader%20Screen/README.md) | Reader screen: continuous webtoon-style page scroll (native Kotlin/Compose `LazyColumn` + Server-Driven UI, diverging from the original `FlashList` design), chapter progress tracking, auto read/unread marking, offline banner, keep-screen-on — all 18 tasks done |
| [017 - Reestruturacao](017%20-%20Reestruturacao/README.md) | Domain restructuring, cross-layer contracts and process safeguards: Kotlin architecture re-split into isolated Gradle modules (`:server` with a nested swappable Kavita plugin, `:content-digest` cache-first Page/Chapter/Series contracts, generic `:cache`/`:preferences`, `:external-metadata-server`), RN data layer reorganized (Layer 4 Services + domain Tools, no per-screen `transforms/`), reader-v2, splash as an RN route (no `SplashActivity`), RN→RN EventBus, `ui_preferences` table dropped, and process safeguards (CLAUDE.md contract-change rule, `checar-arquitetura` skill, `atualizar-changelog` origin-tag cross-check) — all 39 tasks done. The two `_*-design-notes.md` working files are kept as-is (pt-BR/mixed) as the plan's raw design record, not translated. |
| [008 - Notifications & Deep Links](008%20-%20Notifications%20%26%20Deep%20Links/README.md) | Notifications and deep links: `:notifications` module (foreground service + persistent ntfy WebSocket with resilient 5s→1h reconnection backoff), series resolution/filter by Following, per-series native notifications, config sub-screen with live connection status, in-app history tab with per-chapter granularity (Room 17→18), optional collapsing of close chapters of the same series, read-on-consumption via the RN `EventsManager`, and deep links (`mymangareader://` scheme + App Links for the content server's four real URL shapes, translated to internal routes entirely Kotlin-side, with an `ACTION_SEND` fallback since `assetlinks.json` can't be published on a host we don't own) — all 9 tasks done, plus the post-implementation fixes recorded in the plan's `INDEX.md`. |
| [028 - Theme & Design Tokens](028%20-%20Theme%20%26%20Design%20Tokens/README.md) | Theme and design tokens: colour, typography and sizing all come from tokens — a semantic taxonomy where every token carries its own written application rule (`colors.types.ts`), opacity promoted to a separate axis (`ColorTool.add.alpha`), a type scale anchored on the Android default (14) with weights reduced to regular/bold and `line.height` as its own scale, `sizes.ts` (numbered spacing, named radius/border, the `gutter` every screen breathes by, `icon.size`), `ThemeProvider`/`useTheme()` with runtime switching persisted in `:preferences` and a picker in Settings, `createStyles` injecting every token into the `*.styles.ts` files, and an ESLint rule that refuses colour, font and size literals. Kotlin side: the reader's page placeholder now takes its colour and text from RN over SDU (new nodes: Spinner, Pressable, text substitution) with `ColorTool.to.hex` crossing the native boundary. Identity: petróleo became the default theme (the old one became "carmim") and the app's identity colour — icon, native splash, notification — mirrors it, with a test locking that sync. Shipped 12 identities, 4 of them with an OLED variant, plus `scripts/generate-theme.js` and `scripts/build-theme-cards.py` — all 11 tasks done. Tasks 004 and 011 grew well past their original statements (004 absorbed gutter, `line.height`, `icon.size`, the `IconButton` and the conversion of characters into icons; 011 delivered 6 identities instead of 3, plus the tooling). Petróleo and carmim are recorded as inherited exceptions to the AAA legibility bar. |
