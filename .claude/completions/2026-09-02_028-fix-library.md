---
task: 028 - fix-library
plan: 017 - Reestruturação
date: 2026-09-02
status: done
---

# 028 - Correção: Library (remoção do LibraryModule, listagem via Server)

## O que foi entregue

`LibraryModule.kt` deletado (e removido de `AppReactPackage.kt`), junto com todo o listing legado
de série no Kotlin: `KavitaSeriesFeature.listSeries()` / `resolveProgress()` / `SeriesSummary`,
`BffFeature.syncBff()` / `MangaDto`, e o loop de warm-up do `SplashSyncCoordinator` (reescrito
para um no-op). No RN, `frontend/src/shared/bridge/library.ts` e a Library/Following legada
(`LibraryScreen.tsx`, `LibraryService`, `LibraryTransform`, `useLibrary`, `FollowingScreen.tsx`)
foram apagados. A listagem de série passou a ir por `Server.serials.list()` (Layer 2) →
`SerialsService.get()` → `buildSerialsDigest` (Layer 3, cache-first, sem `@Volatile`). Fechada em
conjunto com a Task 036, que detalha o novo caminho.

## Como foi testado

- `npx tsc --noEmit` → 0 erros.
- `npx jest` → 756 testes, 54 suites, todas passando.
- `./gradlew compileDebugKotlin` + `./gradlew koverVerify` → BUILD SUCCESSFUL (piso Kotlin 81).
- `make coverage-js` → pisos JS 68/68/77/90 respeitados.
- **Dispositivo físico real** (`make redeploy-log`, rc48 → rc64): Library e Following carregam
  sem `LibraryModule`; ordenação alfabética e "atualizado recentemente" funcionam; banner de
  frescor aparece; troca Library↔Following sem tela de carregar; índice A-Z não crasha mais.

## Aprovação

Usuário aprovou explicitamente em 2026-09-02 após rodar `make redeploy-log` e testar
Library/Following e a ordenação no device: "show, funcionou, acho que agora da para commitar e
fechar a task".

## Notas

- Divisão 028 (remoção do legado) × 036 (Library sobre digest) ficou artificial — a mesma
  reescrita cobriu as duas; fechadas juntas.
- **Follow-ups não bloqueantes:** 401/token esporádico ao abrir a Library → **Task 035** (auth
  ainda não migrou pro `:server`); "não re-buscar dentro de N min" no nível da lista → task
  separada; warm-up da lista na splash via `seedLibrary()` (já exportado) → task da splash.
- `chapterCacheDao` continua em uso em `SeriesModule.kt` (`getCachedChapters` /
  `markChaptersRead/Unread`) — só a agregação por-série do listing saiu.
