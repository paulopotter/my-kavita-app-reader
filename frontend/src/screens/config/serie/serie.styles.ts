import { StyleSheet } from 'react-native';

// This sub-screen only uses the shared chrome (config.styles) plus ChapterSortConfigFields'
// own styling. Kept as a file for convention; add rules here if the screen grows its own.
export const styles = StyleSheet.create({});
