import { StyleSheet } from 'react-native';

// The debug screen only uses the shared chrome (config.styles) + each Section's own styles.
// Kept as a file for convention.
export const styles = StyleSheet.create({});
